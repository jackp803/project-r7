# E7 Current Task

- task_id: `E7-20260824-052`
- issued_at: `2026-08-24T21:55:00+08:00`
- state: `ACTIVE`
- target_branch: `agent/e7-gate-b-durable-paper-integration-review-20260824`
- authority: `agents/E7_INTEGRATION.md`, `agents/README.md`, `contracts-v0.1`, accepted Gate B in-memory chain through PR #55, PR #57/#58 lifecycle projection contract/producer, PR #60 lifecycle vocabulary clarification, PR #61 E6 durability merge `42f6d015ea5c9387983a822820dde211608a249e`

## Objective

Perform only the bounded E7 **static Gate B durable Paper integration review** now that the E6 persistence/restart/audit implementation is merged.

Review whether the accepted in-memory Gate B path and the merged E6 durable storage surface compose into one contract-coherent, fail-closed Paper runtime definition suitable for later approved-local executable verification.

Do not execute project code in this task and do not promote Gate B.

## Required inspection

Read latest `main` and at minimum:

- `README.md`, `agents/README.md`, `agents/E7_INTEGRATION.md`;
- `contracts/README.md`, `contracts/SHARED_CONTRACTS_V1.md`;
- protection, close/TradeResult, funding, Position lifecycle projection and lifecycle vocabulary profiles plus ADR-0005 through ADR-0008;
- accepted E4/E5/Paper in-memory integration surfaces through PR #55;
- accepted E5 lifecycle projection producer from PR #58;
- merged E6 runtime storage/recovery implementation and tests from PR #61;
- `tests/integration/**`, `tests/e2e/**`, `tests/safety/**` and relevant E4/E5/E6 test definitions;
- `status/INTEGRATION_STATUS.md`, `status/RELEASE_GATES.md`.

## Required review boundary

Confirm statically that the durable graph preserves exact authority and lineage across:

```text
Strategy/Signal
-> TradeIntent
-> RiskDecision
-> ApprovedTradePlan
-> E4 Paper OrderRequest / OrderResult / Fill
-> E5 Position lifecycle projection / PositionAction
-> protection / close / emergency paths
-> FundingAllocationEvidence
-> TradeResult
-> E6 durable journal / restart recovery / audit
```

At minimum review:

1. canonical IDs/lineage are preserved through persistence/restart and never regenerated by E6;
2. E4 remains broker/execution truth authority and E5 remains lifecycle/risk authority;
3. E6 recovery does not infer lifecycle, protection, flatness, PnL, funding zero, or healthy state from unrelated rows;
4. partial fill/nonterminal order truth survives restart without quantity conflation;
5. `UNKNOWN` / `RECONCILIATION_REQUIRED` survives restart and blocks false healthy/current claims;
6. newer raw E4 Position truth beyond the latest E5 lifecycle projection requires E5 re-attestation and does not synthesize a canonical Position;
7. GENESIS/TRANSITION/REATTESTATION revision/predecessor/vocabulary semantics fail closed;
8. funding evidence lineage conflict cannot rewrite an already persisted TradeResult;
9. ordinary EXIT, EMERGENCY_EXIT, and verified full PROTECTION_STOP paths can be represented durably through canonical TradeResult without bypassing E5/E4 authority;
10. corrupt/incomplete/conflicting durable graphs fail closed rather than last-write-wins;
11. no provider/private API, credentials, GitHub CI, strategy lifecycle promotion, PAPER/SHADOW/LIVE authority, or other unrelated scope entered via PR #61.

## Required E7 integration/E2E test definitions

Add or update only E7-owned definitions as needed under `tests/integration/**`, `tests/e2e/**`, and/or `tests/safety/**` so the later approved-local Gate B run has deterministic coverage for at least:

- database close/reopen with open protected Position and active/nonterminal order state;
- partial Fill plus exact protection quantity across restart;
- ambiguous/UNKNOWN order or Position state across restart;
- newer raw broker Position observation causing `REATTESTATION_REQUIRED` without synthetic lifecycle promotion;
- ordinary EXIT -> funding -> TradeResult -> close/reopen exact audit recovery;
- EMERGENCY_EXIT -> funding -> TradeResult -> close/reopen exact audit recovery;
- verified full PROTECTION_STOP -> funding -> TradeResult -> close/reopen exact audit recovery;
- lifecycle gap/predecessor/vocabulary conflict fail-closed behavior;
- funding same-lineage conflict and immutable TradeResult binding;
- incomplete/corrupt graph blocks healthy/reconciled claims;
- no GitHub Actions/CI dependency and no provider/private call.

Reuse real accepted module surfaces. Do not build a parallel fake strategy/risk/execution implementation merely to make integration tests easy.

## Release/status reconciliation

Reconcile E7-owned `status/INTEGRATION_STATUS.md` and `status/RELEASE_GATES.md` to current Git evidence.

The documents must no longer claim that the E5 lifecycle projection producer or E6 durability implementation is absent after PR #58/#61.

However, all executable Gate B criteria that have not run in an allowed environment remain `NOT_RUN` or `BLOCKED` as appropriate.

Required conservative state after this task unless new static blocker is found:

```text
Gate A = PASS / RESEARCH-INTEGRATION ONLY
E5 lifecycle projection producer = MATERIALIZED / executable NOT_RUN
E6 durability implementation = MATERIALIZED / executable NOT_RUN
Restart/persistence executable criterion = NOT_RUN
Paper E2E durable audit executable criterion = NOT_RUN
Gate B = BLOCKED / NOT YET PASS
PAPER / SHADOW / LIVE = UNAUTHORIZED
```

Do not convert static source acceptance into executable PASS.

## Defect / blocker rule

If review finds a reproducible implementation defect under an already settled contract, record exact expected-vs-actual evidence and the responsible domain/Codex boundary; do not silently rewrite another domain's production code.

If review finds an unresolved shared semantic/architecture gap, stop with `BLOCKED / CONTRACT_OR_SEMANTIC_GAP` and exact evidence. Do not invent precedence or weaken fail-closed rules.

## Writable scope

E7-owned only:

- `tests/integration/**`;
- `tests/e2e/**`;
- cross-module `tests/safety/**`;
- `status/INTEGRATION_STATUS.md`;
- `status/RELEASE_GATES.md`;
- E7-specific status/evidence under `status/e7/**`;
- `coordination/E7/STATUS.md` on the target branch;
- `contracts/**` / `docs/adr/**` only if a newly proven shared semantic blocker genuinely requires E7 resolution; otherwise do not change them.

Forbidden:

- E1-E6 production code/tests;
- provider/private API/network/credentials;
- `.github/workflows/**` or GitHub CI/compute;
- strategy promotion or PAPER/SHADOW/LIVE authorization;
- approved-local project execution in this task.

## Executable verification

This task is static integration/release-definition work only.

Record:

```text
project_executable_verification = NOT_RUN
```

Do not run project code/tests and do not request Local Runner execution. Provide exact future local-only commands required for the complete Gate B durable integration matrix. `NOT_RUN != PASS`.

## Acceptance

### DONE

- merged PR #61 durability surface is statically compatible with accepted Gate B E4/E5 contracts and in-memory paths;
- deterministic E7 durable Paper integration/E2E/safety test definitions are complete for the bounded Gate B slice;
- release/integration status is reconciled to current Git evidence without false PASS;
- any remaining criteria requiring execution are explicitly `NOT_RUN` with exact commands;
- no undocumented authority inversion, lifecycle inference, last-write-wins, CI/provider/private scope, or release promotion is introduced;
- if the only remaining blocker is executable evidence, state this explicitly as `READY_FOR_APPROVED_LOCAL_GATE_B_VERIFICATION` while keeping Gate B `BLOCKED / NOT YET PASS`.

### BLOCKED

- an actual contract/architecture/implementation blocker prevents a coherent durable Paper integration definition;
- record exact evidence, responsible owner, and required bounded next action;
- do not compensate with fake adapters, weakened safety, or release promotion.

## Completion / mailbox rule

Commit/push E7-owned integration definitions/status evidence to `agent/e7-gate-b-durable-paper-integration-review-20260824`.

Write/push terminal `coordination/E7/STATUS.md` on that target branch with task_id `E7-20260824-052` and stop.

Do not self-start approved-local verification, Gate C, provider/private APIs, PAPER, SHADOW, LIVE, or another task.