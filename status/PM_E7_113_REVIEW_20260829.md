# PM Review — E7-20260829-113

## Decision

`STATIC REMEDIATION SEMANTICS ACCEPTABLE / MERGE NOT ACCEPTED DUE WRITABLE-SCOPE VIOLATION`

The PM review confirms that the E7-113 source remediation closes the previously identified FP-16 external-consumer participation fail-open defect: non-null caller-supplied `RuntimePreflightAuthority.external_consumer_authority` now contributes to material external participation, so missing matching `external_consumer_evidence` fails closed with the existing `PREFLIGHT_EXTERNAL_CONSUMER_NOT_ACCEPTED` reason. Fixed unconditional role requirements and supervisor-triggered participation remain intact. External evidence without current matching authority also remains fail closed.

However, E7-113 cannot be accepted/merged yet because the authoritative E7-113 TASK Writable scope was an exact file list and permitted regression changes in existing `tests/integration/test_runtime_preflight.py`; it did not authorize creation of a new `tests/integration/test_runtime_preflight_external_consumer_regression.py` file. The branch therefore exceeds the task's explicit writable scope even though the test semantics are appropriate.

## Required bounded remediation

A fresh E7 task may continue the same unmerged branch and perform governance-only test-layout remediation:

1. preserve the accepted E7-113 production source fix unchanged unless a minimal import/test-layout adjustment is mechanically required;
2. move the external-consumer participation regression cases into existing `tests/integration/test_runtime_preflight.py`;
3. delete `tests/integration/test_runtime_preflight_external_consumer_regression.py`;
4. update E7 FP-16 handoff/qualification manifest so future local commands reference only the authorized existing test module;
5. make no shared-contract, provider, AgentBridge, OperationalMode, risk, release, runtime-authority or capital change.

## Verification classification

```text
project executable verification = NOT_RUN / NOT_PASS
FP-16 runtime-preflight tests = NOT_RUN / NOT_PASS
LF-0 = BLOCKED / UNCHANGED
LF-1 = NOT_RUN / NOT_PASS
LF-2 = PARTIAL / NOT PASS
provider requests = 0
credentials = NONE
provider/account mutation = 0
process launch/restart = 0
order/protection actions = 0
SHADOW/PAPER = NOT_AUTHORIZED
10U bounded live-fire = NOT_AUTHORIZED
Gate D / LIVE = BLOCKED / UNAUTHORIZED
capital exposure = NONE
```

`NOT_RUN` is not PASS. This review grants no provider/runtime/capital authority and does not authorize exact-revision preparation or qualification execution.
